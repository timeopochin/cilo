#include "sym.h"

/*
Expr evaluate(Expr* expr) {
    Expr eval;
    eval.hl = false;
    switch (expr->type) {
        case INTEGER:

            // Already evaluated
            eval.a = NULL;
            eval.b = NULL;
            eval.val = expr->val;
            eval.type = INTEGER;
            eval.operator = NONE;
            return expr;

        case OPERATOR:

            // Needs evaluating
            eval.a = evaluate(expr->a);
            eval.b = evaluate(expr->b);

            switch eval.a.type

            break;
        case FUNCTION:
            break;
        default:
            break;
    }
}
*/
